<div class="nav user">

  <% if user_signed_in? %>
    <p><b><%= t('.welcome', user: current_user.email) %></b> |
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    </p>
  <% else %>
    <p>
      <%= link_to t('.register'), new_user_registration_path %> |
      <%= link_to t('.login'), user_session_path %>
    </p>
  <% end %>
</div>